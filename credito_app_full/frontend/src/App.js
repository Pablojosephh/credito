import React, {useState} from "react";import axios from "axios";export default function App(){const [file,setFile]=useState(null);const [result,setResult]=useState(null);const upload=async()=>{if(!file) return alert("Selecione um arquivo");const fd=new FormData();fd.append("file",file);const r=await axios.post("http://localhost:5001/upload_financials",fd);setResult(r.data);}return (<div style={{padding:20,fontFamily:"Arial"}}><h2>Frontend - Crédito Seguro Garantia</h2><input type="file" onChange={(e)=>setFile(e.target.files[0])}/><button onClick={upload}>Enviar</button><pre>{JSON.stringify(result,null,2)}</pre></div>);}